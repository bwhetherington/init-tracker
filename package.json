{
  "name": "init-tracker",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "lazy-iters": "^0.5.0",
    "react": "^16.8.4",
    "react-autocomplete": "^1.8.1",
    "react-dom": "^16.8.4",
    "react-html-parser": "^2.0.2",
    "react-scripts": "^4.0.1",
    "serve": "^11.3.2"
  },
  "scripts": {
    "start": "PUBLIC_URL=. react-scripts start",
    "build": "PUBLIC_URL=. react-scripts build",
    "test": "PUBLIC_URL=. react-scripts test",
    "eject": "PUBLIC_URL=. react-scripts eject",
    "postinstall": "PUBLIC_URL=. react-scripts build",
    "prod": "serve -s ./build/ -l $PORT"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
